package encryption.core.configuration;

import com.aes.encryption.core.resolver.EncryptablePropertyResolver;
import org.springframework.core.env.MutablePropertySources;

public class EncryptablePropertySourceConverter {

    public static void convertPropertySources(InterceptionMode interceptionMode, EncryptablePropertyResolver propertyResolver, MutablePropertySources propertySources) {
    }
}
